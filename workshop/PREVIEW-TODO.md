# TODO — Workshop preview image

**Owner: Juno (user). Status: PENDING — will take the screenshot when the mod is done.**

The Steam Workshop item needs a preview image at `workshop/image.png`:
- **< 1 MB**, square (e.g. 512×512 or 1024×1024 PNG).
- Ideally an in-game shot showing the combat gems (defense/offense) flanking the energy orb —
  the signature feature — with the lethal danger cue visible if possible.

Until this exists, `package-workshop.sh` warns and the `publish-workshop` skill will not do a
public upload. A temporary placeholder may be generated only to unblock a **private** test
upload; swap in the real screenshot before going public.
